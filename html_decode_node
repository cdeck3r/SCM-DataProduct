//Decode html Entities in node.js using html-entities

const Entities = require('html-entities').XmlEntities;
const entities = new Entities();
var str = answer // hier kommt der Antwort String von QnA Maker mit html Encodings rein
console.log(entities.decode(str)); //Gibt den Decodierten String mit Umlauten in der Console aus. Je nach Verwendungszweck natürlich in eine Variable schreiben.
